{
  "name": "metricating-api",
  "version": "1.0.0",
  "description": "A system that collects issue tracker data for metric visualization.",
  "main": "app/index.js",
  "repository": "https://github.com/piperamirez/metricating-api",
  "scripts": {
    "test": "jest --watch -c jest.config.unit.json",
    "test-int": "NODE_ENV=test jest --watch -c jest.config.integration.json",
    "build": "babel --presets es2015 -d dist/ app",
    "start": "npm run build && node dist/index.js",
    "test-ci": "jest -c jest.config.json --coverage",
    "👌": "npm run test-ci && npx eslint app",
    "awesome": "npm run test-ci && npx eslint app",
    "fix": "npx eslint app --fix"
  },
  "author": "The Metricating Team ❤️",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^6.2.0",
    "express": "^4.16.4",
    "express-validator": "^5.3.1",
    "jira-client": "^6.4.1",
    "lodash": "^4.17.11",
    "moment": "^2.23.0",
    "request": "^2.88.0",
    "trello": "https://github.com/piperamirez/trello"
  },
  "devDependencies": {
    "acorn": "^6.0.5",
    "babel-cli": "^6.26.0",
    "babel-eslint": "^10.0.1",
    "babel-preset-es2015": "^6.24.1",
    "eslint": "^5.12.0",
    "eslint-plugin-import": "^2.14.0",
    "eslint-plugin-jest": "^22.1.2",
    "jest": "^23.6.0",
    "jest-when": "^2.2.0",
    "node-mocks-http": "^1.7.3",
    "supertest": "^3.4.2"
  }
}
