class Task {
    constructor(id, issueType, dateEnd, status) {
        this.id = id
        this.issueType = issueType
        this.dateEnd = dateEnd
        this.status = status
    }
}

export default Task